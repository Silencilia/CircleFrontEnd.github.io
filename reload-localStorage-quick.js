// Quick localStorage reload - copy and paste this single line into browser console:
localStorage.removeItem('circle-data'); window.location.reload();

// Alternative: Reset to sample data
// localStorage.removeItem('circle-data'); window.location.reload();

// Alternative: Just reload the page
// window.location.reload();

// Alternative: Use the app's built-in reload function (if available)
// const { reloadFromStorage } = useContacts(); reloadFromStorage();
